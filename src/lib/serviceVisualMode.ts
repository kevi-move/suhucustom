/** Visual CMS snapshots saved under this mode are trusted.
 * Older `visual-v1` snapshots used leaf-index text remap and may be scrambled — ignore them.
 */
export const SERVICE_VISUAL_MODE = "visual-v2";
